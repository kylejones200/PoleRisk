# Performance acceleration dependencies
# Install with: pip install -r requirements-performance.txt

# Core requirements (always needed)
-r requirements.txt

# Performance acceleration
numba>=0.56.0  # JIT compilation for 2-50x speedup on geospatial operations

# Optional: For maximum performance, also build Rust extensions
# cd polerisk_rs && maturin develop --release
